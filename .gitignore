# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Often committed, but can be ignored if preferred by team
pnpm-lock.yaml # Often committed, but can be ignored if preferred by team

# Server specific node_modules (if not covered by root node_modules)
server/node_modules/

# Client specific node_modules (if not covered by root node_modules and not already in client/.gitignore)
client/node_modules/

# Build artifacts / Bundles
client/dist/
client/dist.zip
server/dist/
server/server.zip
client/.vite/
*.env

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear in projects that were downloaded from the internet
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE / Editor specific
.vscode/
.idea/
*.swp
*.swo
*~

# Uploads
server/public/uploads/

# Git
.git/
